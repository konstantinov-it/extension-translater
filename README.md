# Extensions Translator 🌍

Скрипт для генерации локализаций расширений на основе OpenAI. На входе — ваши тексты, на выходе — готовые `messages.json` в папке `_locales`.

## Требования
- Node.js 18+
- ключ OpenAI API

## Быстрый старт

1) Установите зависимости:

```bash
npm install
```

2) Укажите ключ OpenAI:
- через переменную окружения `OPENAI_API_KEY`, или
- вставьте ключ прямо в `index.js` в константу `API_KEY`.

3) Заполните `translation-settings.js`:
- `settings` — название и описания
- `languages` — список языков, `country` и флаг `shouldTranslate`

4) Запустите перевод:

```bash
npm run translate
```

Результат появится в `_locales/<code>/messages.json`.

## Настройка параметров

### Основные тексты и языки
Файл `translation-settings.js` — основной способ управления содержимым и языками.

Поля `languages`:
- `code` — код локали
- `language` — название языка
- `country` — страна для контекста перевода (используется в промпте)
- `shouldTranslate` — нужно ли реально переводить язык через модель

`shouldTranslate`:
- `true` — отправляем на перевод
- `false` — используем исходный текст без перевода

Сейчас `shouldTranslate: true` уже выставлен для самых популярных языков локализации:
`de`, `en`, `es`, `es_419`, `fr`, `id`, `it`, `ja`, `ko`, `nl`, `pl`, `pt_BR`, `pt_PT`, `ru`, `th`, `tr`, `uk`, `vi`, `zh_CN`, `zh_TW`.

### Параметры запуска
Меняются в `translation-settings.js`:
- `outputDir` — папка для `_locales`
- `throttleMs` — пауза между запросами
- `maxRetries` — число ретраев при лимитах
- `retryBaseDelayMs` — базовая задержка ретраев

## Полезные заметки
- `name` ограничен 75 символами, `short_description` — 132 символами.
- Папка `_locales` игнорируется в git по умолчанию.
